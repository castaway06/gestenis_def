Cambiar links del menu superior en masters layouts

- (DONE) Si estoy logueado (bien como admin o socio) y accedo a /Home/login me sale el formulario a rellenar, cambio esto???
	Sol: modifico el controlador de login para que compruebe si la variable de Session es nula => lleva a login, o si Session["IsAdmin"] es
	 true o false para redirigir a home de Admin o de Socio.

- (DONE) Excepción en Tools.sendEmail -> mirar que hacer con la excepción.

- (DONE) Revisar BaseController, las acciones que se realizan OnActionExecuting o OnResultExecuting (se deja OnActionExecuting)

SESGUIR EN GESTION DE RESERVAS USUARIO

--------------------------------------------------------------------------------------------------------
_loginpartial (version inicial, si sigue funcionando puedo borrar esto)
@if ((Object)Session["UserId"]!=null)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Get, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Hello " + Session["Name"].ToString() + "!", "Index", (bool)Session["IsAdmin"] ? "Admin" : "Socio", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Mi cuenta <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
                <li>@Html.ActionLink("Mis Datos", "", "")</li>
                <li>@Html.ActionLink("Cambiar contraseña", "CambiarContrasena", (bool)Session["IsAdmin"] ? "Admin" : "Socio")</li>
            </ul>
        </li>
        
        <li>@Html.ActionLink("Log off","LogOff","Home")</li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Registro", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Home", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}

---------------------------------------------------------------------------------------------------------


